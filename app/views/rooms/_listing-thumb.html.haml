.room.col-sm-6.col-md-3
  = link_to room_path("#{room.id}") do
    .room__image
      %img{_pageexpand_: "216", alt: "", src: "#{room.image.url}"}
    .room__detail
      .room__detail-tag
        #{room.filter.category}.#{room.filter.room_type}.#{room.city}
      .room__detail-title
        #{room.title}
      .room__detail-detail
        1泊あたり料金
        ¥ #{room.price}
      .room__detail-evaluation
        ★★★ 222 ・スーパーホスト



